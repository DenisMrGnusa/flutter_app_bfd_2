name: flutter_app_bfd_2
description: Testing, JSON serialization, and immutables (The Boring Flutter Development Show, Ep. 2)

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:

  http: ^0.12.0+2
  built_value: ^6.7.0
  built_collection: ^4.2.2
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.2

dev_dependencies:
  build_runner: ^1.6.6
  built_value_generator: ^6.7.0
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true